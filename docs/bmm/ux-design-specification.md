# Cometix UX Design Specification

_Created on 2024-11-28 by m1397_
_Generated using BMad Method - Create UX Design Workflow v1.0_

---

## Executive Summary

Cometix 是一款移动端优先的 AI 助手应用，核心体验围绕**流畅、私密、高效**三个关键词展开。设计目标是让用户在移动设备上获得比桌面端更好的 AI 对话体验，同时保持数据隐私的透明可控。

**设计愿景：**
> 让 AI 对话像发消息一样自然，让隐私保护像呼吸一样无感。

---

## 1. Design System Foundation

### 1.1 Design System Choice

**选择：shadcn_flutter + Material 3 双系统融合**

| 决策 | 理由 |
|------|------|
| **主组件库** | shadcn_flutter ^0.0.47 |
| **设计语言** | Material 3 |
| **图标系统** | Material Symbols Icons |
| **字体** | 系统默认（SF Pro / Roboto） |

**为什么选择 shadcn_flutter：**
1. 现代化设计，70+ 精美组件
2. 完整的深色/浅色模式支持
3. 高度可定制，与 Material 3 兼容
4. 类型安全，Flutter 原生

**组件使用策略：**
- 优先使用 shadcn_flutter 组件
- 复杂交互使用 Material 3 原生组件
- 自定义组件遵循 shadcn 设计语言

---

## 2. Core User Experience

### 2.1 Defining Experience

**核心体验三要素：**

```
┌─────────────────────────────────────────────────────────┐
│                    COMETIX 体验金字塔                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                      ┌─────────┐                        │
│                      │  愉悦   │  ← 情感化动画、AI人格   │
│                    ┌─┴─────────┴─┐                      │
│                    │    高效     │  ← 快捷短语、手势     │
│                  ┌─┴─────────────┴─┐                    │
│                  │      流畅       │  ← 60fps、<2秒启动  │
│                ┌─┴─────────────────┴─┐                  │
│                │        安全         │  ← 本地存储、加密  │
│                └─────────────────────┘                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**体验原则：**

| 原则 | 描述 | 实现方式 |
|------|------|----------|
| **即时响应** | 用户操作立即有反馈 | 触觉反馈、微动画 |
| **单手可用** | 核心功能单手可达 | 底部导航、手势操作 |
| **渐进复杂** | 新手简单、高手强大 | 隐藏高级功能 |
| **隐私可见** | 用户知道数据在哪 | 本地存储标识 |

### 2.2 Novel UX Patterns

**创新交互模式：**

#### 1. Zoom Drawer 侧边栏（已实现）

```
┌──────────────────────────────────────┐
│  ┌────────┐                          │
│  │        │  ┌────────────────────┐  │
│  │ 侧边栏  │  │                    │  │
│  │        │  │    主内容区         │  │
│  │ 会话列表 │  │    (缩放 0.95)     │  │
│  │        │  │    (圆角 16px)      │  │
│  │        │  │                    │  │
│  └────────┘  └────────────────────┘  │
└──────────────────────────────────────┘
```

- 打开时主内容缩放至 95%
- 添加 16px 圆角
- 水平位移 80% 屏幕宽度
- 动画时长 300ms，曲线 easeOutCubic

#### 2. 流式打字机效果

```
AI 正在输入...
│
▼
这是 AI 的回答，逐字显示█
                        ↑ 闪烁光标
```

- 逐字显示，模拟打字
- 光标闪烁频率 500ms
- 支持中断和继续

#### 3. 手势操作系统

| 手势 | 操作 | 反馈 |
|------|------|------|
| 左滑消息 | 删除 | 红色背景 + 垃圾桶图标 |
| 右滑消息 | 收藏 | 黄色背景 + 星星图标 |
| 长按消息 | 复制菜单 | 震动 + 弹出菜单 |
| 双击消息 | 编辑重发 | 进入编辑模式 |
| 下拉聊天 | 刷新/加载更多 | 弹性动画 |

#### 4. 快捷短语浮层

```
┌──────────────────────────────────────┐
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐        │
│  │翻译│ │代码│ │总结│ │润色│  →     │
│  └────┘ └────┘ └────┘ └────┘        │
├──────────────────────────────────────┤
│  ┌────────────────────────────┐ ┌──┐ │
│  │ 输入消息...                │ │↑│ │
│  └────────────────────────────┘ └──┘ │
└──────────────────────────────────────┘
```

- 输入框上方横向滚动
- 点击即插入对应提示词
- 支持自定义和排序

---

## 3. Visual Foundation

### 3.1 Color System

**主色调：温暖中性**

| 颜色 | 色值 | 用途 |
|------|------|------|
| **Primary** | `#E4D5D5` | 主色调、品牌色 |
| **Accent** | `#8B5E3C` | 强调色、CTA |
| **Background Light** | `#FFFFFF` | 浅色背景 |
| **Background Dark** | `#121212` | 深色背景 |
| **Card Light** | `#F5F5F5` | 浅色卡片 |
| **Card Dark** | `#1E1E1E` | 深色卡片 |

**灰度色阶：**

| 名称 | 色值 | 用途 |
|------|------|------|
| Gray 100 | `#F5F5F5` | 背景、分割线 |
| Gray 200 | `#EEEEEE` | 禁用状态 |
| Gray 300 | `#E0E0E0` | 边框 |
| Gray 400 | `#BDBDBD` | 占位符文字 |
| Gray 500 | `#9E9E9E` | 次要文字 |
| Gray 600 | `#757575` | 辅助文字 |
| Gray 700 | `#616161` | 正文 |
| Gray 800 | `#424242` | 标题 |
| Gray 900 | `#212121` | 主要文字 |

**语义色：**

| 语义 | 浅色模式 | 深色模式 | 用途 |
|------|----------|----------|------|
| Success | `#4CAF50` | `#81C784` | 成功状态 |
| Warning | `#FF9800` | `#FFB74D` | 警告状态 |
| Error | `#F44336` | `#E57373` | 错误状态 |
| Info | `#2196F3` | `#64B5F6` | 信息提示 |

**消息气泡色：**

| 角色 | 浅色模式 | 深色模式 |
|------|----------|----------|
| 用户消息 | `#E4D5D5` | `#3D3D3D` |
| AI 消息 | `#FFFFFF` | `#1E1E1E` |

---

## 4. Design Direction

### 4.1 Chosen Design Approach

**设计方向：温暖极简主义 (Warm Minimalism)**

**核心特征：**
- 大量留白，呼吸感
- 圆角元素（16-24px）
- 柔和阴影
- 温暖中性色调
- 微妙动画

**视觉层级：**

```
┌─────────────────────────────────────────────────────────┐
│  层级 1: 背景层                                          │
│  ├─ 纯色背景 (#FFFFFF / #121212)                        │
│                                                         │
│  层级 2: 容器层                                          │
│  ├─ 卡片 (elevation: 1, radius: 16px)                   │
│  ├─ 侧边栏 (elevation: 2)                               │
│                                                         │
│  层级 3: 内容层                                          │
│  ├─ 消息气泡 (radius: 16px)                             │
│  ├─ 输入框 (radius: 24px)                               │
│                                                         │
│  层级 4: 浮层                                            │
│  ├─ 弹窗 (elevation: 8, radius: 16px)                   │
│  ├─ 底部菜单 (elevation: 16)                            │
│  ├─ Toast (elevation: 4)                                │
└─────────────────────────────────────────────────────────┘
```

**圆角规范：**

| 元素 | 圆角 |
|------|------|
| 按钮 | 12px |
| 卡片 | 16px |
| 输入框 | 24px |
| 消息气泡 | 16px |
| 弹窗 | 16px |
| 头像 | 50% (圆形) |

---

## 5. User Journey Flows

### 5.1 Critical User Paths

#### Flow 1: 首次启动

```
启动画面 (Logo + 加载)
    │ <2秒
    ▼
欢迎页 (可选)
    │ 跳过/开始
    ▼
主聊天界面
    │
    ├─→ 侧边栏 (左滑/点击菜单)
    │       ├─ 新建对话
    │       ├─ 历史对话列表
    │       └─ 设置入口
    │
    └─→ 输入区域
            ├─ 文本输入
            ├─ 语音输入 (P1)
            ├─ 图片上传 (P0)
            └─ 快捷短语
```

#### Flow 2: 发送消息

```
输入文本
    │
    ├─→ 点击发送按钮
    │       │
    │       ▼
    │   显示用户消息气泡
    │       │
    │       ▼
    │   显示 AI 思考状态 (头像动画)
    │       │
    │       ▼
    │   流式显示 AI 回答
    │       │
    │       ▼
    │   回答完成 (光标消失)
    │
    └─→ 点击快捷短语
            │
            ▼
        插入提示词到输入框
            │
            ▼
        自动发送 / 等待编辑
```

#### Flow 3: 管理对话

```
主界面
    │
    ├─→ 左滑打开侧边栏
    │       │
    │       ├─→ 点击历史对话 → 切换对话
    │       │
    │       ├─→ 左滑对话项 → 删除确认
    │       │
    │       └─→ 点击 + 号 → 新建对话
    │
    └─→ 长按消息
            │
            ├─→ 复制
            ├─→ 收藏
            ├─→ 编辑重发
            └─→ 删除
```

#### Flow 4: 设置配置

```
侧边栏 → 设置
    │
    ├─→ 外观设置
    │       ├─ 主题切换 (浅色/深色/跟随系统)
    │       ├─ 字体大小
    │       └─ 气泡样式
    │
    ├─→ AI 设置
    │       ├─ API Key 配置
    │       ├─ 默认模型选择
    │       └─ 助手管理
    │
    ├─→ 隐私设置
    │       ├─ 生物识别锁
    │       ├─ 数据导出
    │       └─ 清除数据
    │
    └─→ 关于
            ├─ 版本信息
            └─ 反馈入口
```

---

## 6. Component Library

### 6.1 Component Strategy

**组件分层：**

| 层级 | 组件类型 | 示例 |
|------|----------|------|
| **原子** | 基础元素 | Button, Icon, Text, Avatar |
| **分子** | 组合元素 | InputBar, MessageBubble, QuickPhraseChip |
| **有机体** | 功能模块 | ChatList, Sidebar, SettingsSection |
| **模板** | 页面布局 | HomeScreen, ChatScreen, SettingsScreen |

**核心自定义组件：**

#### 1. MessageBubble

```dart
MessageBubble(
  message: Message,
  isUser: bool,
  onLongPress: () => showMenu(),
  onSwipeLeft: () => delete(),
  onSwipeRight: () => favorite(),
)
```

#### 2. InputBar

```dart
InputBar(
  controller: TextEditingController,
  onSend: (String) => void,
  onImagePick: () => void,
  onVoiceStart: () => void,
  quickPhrases: List<QuickPhrase>,
)
```

#### 3. ZoomDrawer

```dart
ZoomDrawer(
  isOpen: bool,
  onToggle: () => void,
  sidebar: Widget,
  content: Widget,
  scaleRatio: 0.95,
  slideRatio: 0.8,
)
```

#### 4. AIAvatar

```dart
AIAvatar(
  isThinking: bool,  // 思考时显示动画
  size: 40,
)
```

---

## 7. UX Pattern Decisions

### 7.1 Consistency Rules

**导航模式：**
- 主导航：Zoom Drawer 侧边栏
- 次级导航：底部 Tab（设置页面内）
- 返回：系统返回手势 + AppBar 返回按钮

**反馈模式：**

| 操作 | 反馈类型 | 时机 |
|------|----------|------|
| 点击按钮 | 涟漪效果 + 震动 | 立即 |
| 发送消息 | 消息气泡出现动画 | 立即 |
| AI 思考 | 头像呼吸动画 | 发送后 |
| 操作成功 | Toast 提示 | 完成后 |
| 操作失败 | 红色 Toast + 震动 | 失败时 |

**加载状态：**

| 场景 | 加载方式 |
|------|----------|
| 启动 | Logo + 进度条 |
| AI 回答 | 流式显示 + 光标 |
| 图片上传 | 缩略图 + 进度环 |
| 列表加载 | Skeleton 骨架屏 |

**空状态：**

| 场景 | 显示内容 |
|------|----------|
| 无对话历史 | 插图 + "开始你的第一次对话" |
| 无搜索结果 | 插图 + "没有找到相关内容" |
| 网络错误 | 插图 + "网络连接失败" + 重试按钮 |

---

## 8. Responsive Design & Accessibility

### 8.1 Responsive Strategy

**断点定义：**

| 断点 | 宽度 | 布局 |
|------|------|------|
| Mobile | < 600px | 单列，Drawer 导航 |
| Tablet | 600-1024px | 双列，侧边栏常驻 |
| Desktop | > 1024px | 三列，多面板 |

**移动端优先：**
- 默认设计针对 375px 宽度
- 触摸目标最小 44x44pt
- 底部安全区域适配

### 8.2 Accessibility

| 要求 | 实现 |
|------|------|
| 对比度 | WCAG AA (4.5:1) |
| 字体缩放 | 支持 0.8x - 1.5x |
| 屏幕阅读器 | Semantics 标签 |
| 动画减弱 | 尊重系统设置 |
| 键盘导航 | Web 端支持 |

---

## 9. Implementation Guidance

### 9.1 Completion Summary

**已实现组件：**
- ✅ Zoom Drawer 侧边栏
- ✅ 基础聊天界面
- ✅ 消息气泡（Markdown 渲染）
- ✅ 输入栏组件
- ✅ 快捷短语系统
- ✅ 深色/浅色主题
- ✅ shadcn_flutter 集成

**待实现组件：**
- 🚧 手势操作（左滑删除、右滑收藏）
- 🚧 AI 头像动画
- 🚧 流式打字机光标效果
- 🚧 图片上传 UI
- 🚧 语音输入 UI
- 🚧 设置页面完善
- 🚧 空状态设计
- 🚧 加载骨架屏

**开发优先级：**

| 优先级 | 组件 | 预计工作量 |
|--------|------|------------|
| P0 | 手势操作系统 | 2-3天 |
| P0 | 本地存储 UI 反馈 | 1天 |
| P0 | 图片上传 UI | 2天 |
| P1 | AI 头像动画 | 1天 |
| P1 | 打字机光标效果 | 0.5天 |
| P1 | 语音输入 UI | 2天 |
| P2 | 空状态插图 | 1天 |
| P2 | 骨架屏 | 1天 |

---

## Appendix

### Related Documents

- Product Requirements: `docs/bmm/prd.md`
- Brainstorming: `docs/bmm/bmm-brainstorming-session-2024-11-28.md`
- Research: `docs/bmm/research-comprehensive-2024-11-28.md`
- Project Documentation: `docs/bmm/index.md`

### Design Tokens (Flutter)

```dart
// lib/theme/app_theme.dart

class AppTheme {
  // Colors
  static const primaryColor = Color(0xFFE4D5D5);
  static const accentBrown = Color(0xFF8B5E3C);
  static const backgroundLight = Color(0xFFFFFFFF);
  static const backgroundDark = Color(0xFF121212);

  // Radius
  static const radiusSmall = 8.0;
  static const radiusMedium = 12.0;
  static const radiusLarge = 16.0;
  static const radiusXLarge = 24.0;

  // Spacing
  static const spacingXS = 4.0;
  static const spacingS = 8.0;
  static const spacingM = 16.0;
  static const spacingL = 24.0;
  static const spacingXL = 32.0;

  // Animation
  static const animationFast = Duration(milliseconds: 150);
  static const animationNormal = Duration(milliseconds: 300);
  static const animationSlow = Duration(milliseconds: 500);
}
```

### Version History

| Date | Version | Changes | Author |
|------|---------|---------|--------|
| 2024-11-28 | 1.0 | Initial UX Design Specification | m1397 |

---

_This UX Design Specification was created through collaborative design facilitation, not template generation. All decisions were made with user input and are documented with rationale._
